import yaml


def create_topology(contents):
    #create NS-3 network topology out of the yaml file

def load_yaml(config):
    #load the contents of the yaml file into memory


def run(config):
    contents = load_yaml(config)
    return create_topology(contents)
